'use strict'

import * as generator from './generator'

// -------------------------------------------------------------
// Tests.
// -------------------------------------------------------------

describe('todo()', () => {
  it('should do…', () => {
    expect(generator).toBeDefined()
  })
})
